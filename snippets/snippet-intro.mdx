Принцип DRY («не повторяйся») применим и к документации.
Если один и тот же текст используется на нескольких страницах,
вынесите его в отдельный фрагмент: так формулировка останется
одинаковой и будет обновляться в одном месте.
